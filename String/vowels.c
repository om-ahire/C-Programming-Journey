#include <stdio.h>
int main()
{
    int i, count = 0;
    char input[100];
    printf("Enter the String : ");
    gets(input);

    while (input[i] != '\0')
    {
        if (
            input[i] == 'a' || input[i] == 'A' ||
            input[i] == 'i' || input[i] == 'I' ||
            input[i] == 'o' || input[i] == 'O' ||
            input[i] == 'u' || input[i] == 'U' ||)
            ;
    }
}